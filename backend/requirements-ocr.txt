# Local OCR for scans and photos: PaddleOCR models on ONNX Runtime, CPU-only, offline once installed.
# Requires a Python with an onnxruntime wheel (3.11–3.12 at the time of writing). Optional: without it
# tables, text and text-layer PDFs are still read and /api/health reports "ocr": "unavailable".
rapidocr-onnxruntime>=1.3
pypdfium2>=4.30
pillow>=10.1
