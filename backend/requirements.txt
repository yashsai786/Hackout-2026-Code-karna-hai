# Leakpoint API. Every package here is imported by backend/ — nothing is aspirational.
fastapi==0.110.1
uvicorn==0.25.0
pydantic>=2.6.4
python-dotenv>=1.0.1

# Loads the model artefact trained by ai/train.py.
joblib>=1.3.0
numpy>=1.26.0
pandas>=2.2.0
scikit-learn>=1.4.0

# Optional persistence. The service runs without MongoDB; see server.py lifespan.
# pymongo is pinned because motor 3.3.1 imports a private symbol newer releases removed.
motor==3.3.1
pymongo==4.6.3

# Test suite.
pytest>=8.0.0
requests>=2.31.0
