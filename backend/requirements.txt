# Leakpoint API. Every package here is imported by backend/ — nothing is aspirational.
fastapi==0.110.1
uvicorn==0.25.0
pydantic>=2.6.4
python-dotenv>=1.0.1

# Loads the model artefact trained by ai/train.py.
joblib>=1.3.0
numpy>=1.26.0
pandas>=2.2.0
# Pinned narrowly: the committed artefact is a pickle and will not load across a major sklearn bump.
scikit-learn>=1.9.1,<1.10

# Optional persistence. The service runs without MongoDB; see server.py lifespan.
# pymongo is pinned because motor 3.3.1 imports a private symbol newer releases removed.
motor==3.3.1
pymongo==4.6.3

# Document intake: multipart uploads, spreadsheets and text-layer PDFs.
python-multipart>=0.0.9
openpyxl>=3.1
pypdf>=4.0

# Local OCR for scans and photos: PaddleOCR models on ONNX Runtime, CPU-only, offline once installed.
rapidocr-onnxruntime>=1.3
pypdfium2>=4.30
pillow>=10.1

# Test suite.
pytest>=8.0.0
requests>=2.31.0
