# Training the hotspot-disaggregation model. Install this only to retrain; the committed
# artefact in ai/models/ means the demonstration runs without it.
numpy>=1.26.0
pandas>=2.2.0
scikit-learn>=1.4.0
joblib>=1.3.0
pytest>=8.0.0
